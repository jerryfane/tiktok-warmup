GOOGLE_GENERATIVE_AI_API_KEY=AIz..............

# Proxy pool (comma or newline separated)
# Supported formats: host:port, user:pass@host:port, host:port:user:pass
# Alternative to proxies.json / proxies.txt — each device gets a proxy round-robin
# PROXY_POOL=proxy1.example.com:8080,proxy2.example.com:3128